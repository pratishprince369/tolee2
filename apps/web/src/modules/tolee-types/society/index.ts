import { TOLEE_TYPE_REGISTRY } from '../registry';

export const SocietyModule = {
  config: TOLEE_TYPE_REGISTRY.society,
  roles: TOLEE_TYPE_REGISTRY.society.roles,
  features: TOLEE_TYPE_REGISTRY.society.features,
  aiAssistant: TOLEE_TYPE_REGISTRY.society.aiAssistant
};
