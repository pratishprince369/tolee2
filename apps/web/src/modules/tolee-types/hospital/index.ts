import { TOLEE_TYPE_REGISTRY } from '../registry';

export const HospitalModule = {
  config: TOLEE_TYPE_REGISTRY.hospital,
  roles: TOLEE_TYPE_REGISTRY.hospital.roles,
  features: TOLEE_TYPE_REGISTRY.hospital.features,
  aiAssistant: TOLEE_TYPE_REGISTRY.hospital.aiAssistant
};
