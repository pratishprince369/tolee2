import { TOLEE_TYPE_REGISTRY } from '../registry';

export const BoardMeetingModule = {
  config: TOLEE_TYPE_REGISTRY.board_meeting,
  roles: TOLEE_TYPE_REGISTRY.board_meeting.roles,
  features: TOLEE_TYPE_REGISTRY.board_meeting.features,
  aiAssistant: TOLEE_TYPE_REGISTRY.board_meeting.aiAssistant
};
